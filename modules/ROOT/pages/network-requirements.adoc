= Network Requirements for Client Connectivity

[abstract]
Planning your networking configuration should be one of the first tasks performed when designing your Couchbase deployment.
This page summarizes the supported options for connecting clients (including XDCR) to your Couchbase cluster.

The following simplified diagrams depict example client configuration strings, required DNS records, and the Couchbase cluster map as advertised by Couchbase Server.
For more detailed documentation, please consult the xref:networking.adoc[networking] and xref:xdcr.adoc[XDCR] pages.

In general:

* Diagrams depicting Couchbase nodes being advertised with IP addresses or non-default port numbers (3xxxx) require the xref:couchbase-cluster-config.adoc#exposedfeatures[exposed features] configuration parameter to be specified.
* Diagrams depicting Couchbase nodes being advertised with non-default DNS addresses require the xref:couchbase-cluster-config.adoc#spec-dns[DNS] configuration parameter to be specified.

== In-cluster Networking

This is the simplest form of client connectivity.
Clients are all located in the same Kubernetes cluster as the Couchbase cluster.

image::network-in-cluster.png[In-cluster Networking]

&check; Supports all https://github.com/containernetworking/cni[CNI^] network types

&check; Supports TLS

&check; Supports SRV-based service discovery

&cross; Supports addressability from the Internet

== Out-of-cluster Networking with Private IP-based Addressing

This form of client connectivity uses Kubernetes `NodePort` services to connect to the Couchbase cluster.
The Operator ensures that `NodePort` addresses are synchronized with Couchbase Server so that they are correctly advertised to clients.

image::network-external-ip.png[Out-of-cluster Networking with Private IP-based Addressing]

&check; Supports all https://github.com/containernetworking/cni[CNI^] network types

&cross; Supports TLS 

&cross; Supports SRV-based service discovery

&cross; Supports addressability from the Internet

== Out-of-Cluster Networking with Private IP-based Addressing and Kubernetes DNS

This form of client connectivity uses DNS to connect to the Couchbase cluster.
As `Pod` IP addresses are used to connect to the Couchbase cluster, you cannot use overlay networking.
A third-party solution is required to replicate `Pod` DNS names and `Pod` IP addresses into the client Kubernetes cluster.

image::network-external-kubedns.png[Out-of-Cluster Networking with Private IP-based Addressing and Kubernetes DNS]

&cross; Supports all https://github.com/containernetworking/cni[CNI^] network types

&check; Supports TLS 

&check; Supports SRV-based service discovery (if supported by the DNS replicator)

&cross; Supports addressability from the Internet

== Out-of-cluster Networking with Private IP-based Addressing and Dynamic DNS

This form of client connectivity uses DNS to connect to the Couchbase cluster with Kubernetes `NodePort` services.
A third-party solution is required to replicate `Pod` DNS names and `NodePort` IP addresses into the client Kubernetes cluster.

image::network-external-ddns-private.png[Out-of-cluster Networking with Private IP-based Addressing and Dynamic DNS]

&check; Supports all https://github.com/containernetworking/cni[CNI^] network types

&check; Supports TLS

&check; Supports SRV-based service discovery (if supported by the DNS replicator)

&cross; Supports addressability from the Internet

NOTE: This form of client connectivity is compatible with the Kubernetes https://github.com/kubernetes-incubator/external-dns[external-dns^] project.

[#public-connectivity-ddns]
== Public Connectivity: Out-of-cluster Networking with Public IP-based Addressing and Dynamic DNS

This form of client connectivity uses DNS to connect to the Couchbase cluster over public IP addresses.
Couchbase Server nodes are exposed with `LoadBalancer` services.
A third-party solution is required to replicate `Pod` DNS names and `Service` IP addresses into the client Kubernetes cluster.

image::network-external-ddns-public.png[Out-of-cluster Networking with Public IP-based Addressing and Dynamic DNS]

&check; Supports all https://github.com/containernetworking/cni[CNI^] network types

&check; Supports TLS

&check; Supports SRV-based service discovery (if supported by the DNS replicator)

&check; Supports addressability from the Internet

NOTE: This form of client connectivity is compatible with the Kubernetes https://github.com/kubernetes-incubator/external-dns[external-dns^] project.